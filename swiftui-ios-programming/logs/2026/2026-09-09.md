---
template_version: "v03"
date: 2026-09-09
book: swiftui-ios-programming
study_day: 12
elapsed_day: 26
duration_minutes: null
status: planned
tags: []
---

# 2026-09-09

## 기본 정보

- 누적 실학습일: 12일
- 시작 후 경과일: 26일차
- 오늘의 목표: 우선은 chapter 09 끝내기.
- 총 학습 시간:


## 세션 1

- 시작 시간: `10:50` (추정)
- 종료 시간: `11:12`
- 공부 시간: 22분

#### 09.15 약식 인수 이름

* **약식 인수 이름(shorthand argument name)**
    - 클로저 표현식이 더 이상 인수 또는 반환 유형을 정의하지 않기 때문에 타입 애너테이션인 ((String, String) -> String)이 할당 연산자의 왼쪽으로 이동되었다.

#### 09.16 스위프트의 클로저

컴퓨터 공학 용어에서의 **클로저**는 함수나 클로저 표현식과 같은 독립적인 코드 블록과 코드 블록 주변에 있는 하나 이상의 변수가 결합된 것을 말함.

- 변수를 잡고 있다(captured), 또는 가두고 있다(closed over)

스위프트와 관련해서 **클로저**와 **클로저 표현식** 용어가 넓게 쓰이다 보니 횬용되기 시작. 중요한 점은 스위프트는 둘 다 지원한다.

(chatgpt 참고)
**클로저(closure)**는 하나의 기능/값 자체를 가리키는 넓은 개념이고,
**클로저 표현식(closure expression)**은 그 클로저를 코드로 직접 작성하는 문법입니다.

## 클로저와 클로저 표현식

| 구분 | 예시 | 의미 |
| --- | --- | --- |
| 함수 선언 | `func add(...) { ... }` | 이름이 있는 함수 정의 |
| 클로저 표현식 | `{ (a, b) in ... }` | 이름 없이 클로저를 작성하는 문법 |
| 클로저 | 함수/클로저 표현식 등을 포함하는 더 넓은 개념 | 실행 가능한 기능을 하나의 값처럼 다루는 것 |
| 클로저 타입 | `(Int, Int) -> Int` | 입력과 출력의 형태 |

특히 책을 읽을 때는 **"클로저"와 "클로저 표현식"을 완전히 별개의 것으로 생각하면 오히려 헷갈릴 수 있다.**

다음처럼 생각하면 된다.

```text
클로저
 ├─ 전역 함수
 ├─ 중첩 함수
 └─ 클로저 표현식으로 만든 익명 클로저
```

* 클로저 표현식에서 Swift가 반환 타입을 추론할 수 있다.

#### 09.17 요약
함수, 클로저, 그리고 클로저 표현식은 독립된 코드 블록으로 특정 작업을 수행하기 위하여 호출될 수 있음. 코드를 구조화하고 재사용하는 메커니즘을 제공함. 이번 장은 선언과 구현 관점에서 함수와 클로저의 개념을 설명하였음.

## 세션 2

- 시작 시간: `HH:MM`
- 종료 시간: `HH:MM`
- 공부 시간: N분



## 하루 마무리

- 오늘의 한 줄:
- 오늘의 상태: `완료` / `진행 중` / `막힘`
- 다음 시작점:
